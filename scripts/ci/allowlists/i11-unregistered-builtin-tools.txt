# I11 allowlist — packages under core/tools/ that declare a `kenaz__*`
# builtin tool name and are NOT imported by core/rpc/builtins_wiring.go,
# the single wiring site for the builtin registry.
#
# Enforced by scripts/ci/check-builtin-tool-registration.sh. Seeded by the
# 2026-08-14 unwired sweep (model-moves-transcript-01PMCH01 WP00).
#
# Format: one package path per line, relative to the module root.
#
# CONTRACT (same as i10-unwired-gates.txt): a line that stays must say
# why it stays, dated, naming the blocker and the change that will delete
# it. Lines shrink monotonically. "We'll get to it" is not a reason.
#
# ─────────────────────────────────────────────────────────────────────

# core/tools/monitor  (kenaz__monitor) — DELETED subagent-control-and-
#   background-tasks-01PMZB11 UNIT-3/UNIT-5. The blocker this entry
#   named is fixed: bash.Options.BackgroundSpawn/BackgroundWriters/
#   BackgroundSetPID/BackgroundEnd are now assigned in
#   core/rpc/builtins_wiring.go (backed by the *coretasks.Registry
#   constructed in core/rpc/api.go), spawnBackground allocates the task
#   id BEFORE cmd.Start() and attaches the stdout/stderr writers ahead
#   of it, and Registry.AppendLine (previously a zero-non-test-caller
#   orphan) is now wired from lineWriter.Write so Registry.Tail returns
#   real lines. kenaz__monitor is registered with an explicit
#   builtinEnabledPredicate case, conditioned on the same non-nil
#   taskReg. See core/rpc/background_task_wiring_test.go for the
#   production-wiring-shaped proof.
