schema_version: "1"
archetype: control
category: control
description: "Flow-control primitives."
callable: false
ports:
  inputs:
    - { name: signal, type: any }
  outputs:
    - { name: next, type: any }
budget: none
