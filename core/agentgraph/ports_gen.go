// Code generated by agentgraph/nodes/cmd/gen; DO NOT EDIT.

// This file is auto-generated by core/agentgraph/nodes/cmd/gen.
// Do not hand-edit; run `go generate ./...` from the repo
// root to refresh.

package agentgraph

import (
	"fmt"
)

// ---- Activity port types ----

// ActivityInputs is the typed input port surface for `kind: activity`.
// Use ReadActivityInputs to populate it from a PortValues map.
type ActivityInputs struct {
	In any
}

// ActivityOutputs is the typed output port surface for `kind: activity`.
// Call ToPortValues to convert back to the wire format.
type ActivityOutputs struct {
	Out any
}

// ReadActivityInputs extracts the typed inputs for `kind: activity` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadActivityInputs(pv PortValues) (ActivityInputs, error) {
	var out ActivityInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts ActivityOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ActivityOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- Approval port types ----

// ApprovalInputs is the typed input port surface for `kind: approval`.
// Use ReadApprovalInputs to populate it from a PortValues map.
type ApprovalInputs struct {
	In any
}

// ApprovalOutputs is the typed output port surface for `kind: approval`.
// Call ToPortValues to convert back to the wire format.
type ApprovalOutputs struct {
	Approved any
	Rejected any
}

// ReadApprovalInputs extracts the typed inputs for `kind: approval` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadApprovalInputs(pv PortValues) (ApprovalInputs, error) {
	var out ApprovalInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts ApprovalOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ApprovalOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 2)
	pv["approved"] = o.Approved
	pv["rejected"] = o.Rejected
	return pv
}

// ---- Artifact port types ----

// ArtifactInputs is the typed input port surface for `kind: artifact`.
// Use ReadArtifactInputs to populate it from a PortValues map.
type ArtifactInputs struct {
	In any
}

// ArtifactOutputs is the typed output port surface for `kind: artifact`.
// Call ToPortValues to convert back to the wire format.
type ArtifactOutputs struct {
	Ack any
}

// ReadArtifactInputs extracts the typed inputs for `kind: artifact` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadArtifactInputs(pv PortValues) (ArtifactInputs, error) {
	var out ArtifactInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts ArtifactOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ArtifactOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["ack"] = o.Ack
	return pv
}

// ---- Ask port types ----

// AskInputs is the typed input port surface for `kind: ask`.
// Use ReadAskInputs to populate it from a PortValues map.
type AskInputs struct {
	Context any
}

// AskOutputs is the typed output port surface for `kind: ask`.
// Call ToPortValues to convert back to the wire format.
type AskOutputs struct {
	Answer any
}

// ReadAskInputs extracts the typed inputs for `kind: ask` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadAskInputs(pv PortValues) (AskInputs, error) {
	var out AskInputs

	if raw, ok := pv["context"]; ok {
		out.Context = raw
	}
	return out, nil
}

// ToPortValues converts AskOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o AskOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["answer"] = o.Answer
	return pv
}

// ---- Attachment port types ----

// AttachmentInputs is the typed input port surface for `kind: attachment`.
// Use ReadAttachmentInputs to populate it from a PortValues map.
type AttachmentInputs struct {
	In any
}

// AttachmentOutputs is the typed output port surface for `kind: attachment`.
// Call ToPortValues to convert back to the wire format.
type AttachmentOutputs struct {
	Block any
}

// ReadAttachmentInputs extracts the typed inputs for `kind: attachment` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadAttachmentInputs(pv PortValues) (AttachmentInputs, error) {
	var out AttachmentInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts AttachmentOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o AttachmentOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["block"] = o.Block
	return pv
}

// ---- Branch port types ----

// BranchInputs is the typed input port surface for `kind: branch`.
// Use ReadBranchInputs to populate it from a PortValues map.
type BranchInputs struct {
	Parent any
}

// BranchOutputs is the typed output port surface for `kind: branch`.
// Call ToPortValues to convert back to the wire format.
type BranchOutputs struct {
	BranchId any
}

// ReadBranchInputs extracts the typed inputs for `kind: branch` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadBranchInputs(pv PortValues) (BranchInputs, error) {
	var out BranchInputs

	if raw, ok := pv["parent"]; ok {
		out.Parent = raw
	}
	return out, nil
}

// ToPortValues converts BranchOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o BranchOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["branch_id"] = o.BranchId
	return pv
}

// ---- Checkpoint port types ----

// CheckpointInputs is the typed input port surface for `kind: checkpoint`.
// Use ReadCheckpointInputs to populate it from a PortValues map.
type CheckpointInputs struct {
	In any
}

// CheckpointOutputs is the typed output port surface for `kind: checkpoint`.
// Call ToPortValues to convert back to the wire format.
type CheckpointOutputs struct {
	Ack any
}

// ReadCheckpointInputs extracts the typed inputs for `kind: checkpoint` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadCheckpointInputs(pv PortValues) (CheckpointInputs, error) {
	var out CheckpointInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts CheckpointOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o CheckpointOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["ack"] = o.Ack
	return pv
}

// ---- Compact port types ----

// CompactInputs is the typed input port surface for `kind: compact`.
// Use ReadCompactInputs to populate it from a PortValues map.
type CompactInputs struct {
	Input []Message
}

// CompactOutputs is the typed output port surface for `kind: compact`.
// Call ToPortValues to convert back to the wire format.
type CompactOutputs struct {
	Result any
}

// ReadCompactInputs extracts the typed inputs for `kind: compact` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadCompactInputs(pv PortValues) (CompactInputs, error) {
	var out CompactInputs

	if raw, ok := pv["input"]; ok {
		switch v := raw.(type) {
		case []Message:
			out.Input = v
		case nil:
			// nil is treated as absent
		default:
			return out, fmt.Errorf("compact: port input: cannot cast %T to []Message", raw)
		}
	}
	return out, nil
}

// ToPortValues converts CompactOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o CompactOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- CorpusRead port types ----

// CorpusReadInputs is the typed input port surface for `kind: corpus_read`.
// Use ReadCorpusReadInputs to populate it from a PortValues map.
type CorpusReadInputs struct {
	Query any
}

// CorpusReadOutputs is the typed output port surface for `kind: corpus_read`.
// Call ToPortValues to convert back to the wire format.
type CorpusReadOutputs struct {
	Hits any
}

// ReadCorpusReadInputs extracts the typed inputs for `kind: corpus_read` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadCorpusReadInputs(pv PortValues) (CorpusReadInputs, error) {
	var out CorpusReadInputs

	if raw, ok := pv["query"]; ok {
		out.Query = raw
	}
	return out, nil
}

// ToPortValues converts CorpusReadOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o CorpusReadOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["hits"] = o.Hits
	return pv
}

// ---- CorpusWrite port types ----

// CorpusWriteInputs is the typed input port surface for `kind: corpus_write`.
// Use ReadCorpusWriteInputs to populate it from a PortValues map.
type CorpusWriteInputs struct {
	In any
}

// CorpusWriteOutputs is the typed output port surface for `kind: corpus_write`.
// Call ToPortValues to convert back to the wire format.
type CorpusWriteOutputs struct {
	JobHandle any
}

// ReadCorpusWriteInputs extracts the typed inputs for `kind: corpus_write` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadCorpusWriteInputs(pv PortValues) (CorpusWriteInputs, error) {
	var out CorpusWriteInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts CorpusWriteOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o CorpusWriteOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["job_handle"] = o.JobHandle
	return pv
}

// ---- Decision port types ----

// DecisionInputs is the typed input port surface for `kind: decision`.
// Use ReadDecisionInputs to populate it from a PortValues map.
type DecisionInputs struct {
	In any
}

// DecisionOutputs is the typed output port surface for `kind: decision`.
// Call ToPortValues to convert back to the wire format.
type DecisionOutputs struct {
	True  any
	False any
}

// ReadDecisionInputs extracts the typed inputs for `kind: decision` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadDecisionInputs(pv PortValues) (DecisionInputs, error) {
	var out DecisionInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	} else {
		return out, fmt.Errorf("decision: required input port in is absent")
	}
	return out, nil
}

// ToPortValues converts DecisionOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o DecisionOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 2)
	pv["true"] = o.True
	pv["false"] = o.False
	return pv
}

// ---- Escalate port types ----

// EscalateInputs is the typed input port surface for `kind: escalate`.
// Use ReadEscalateInputs to populate it from a PortValues map.
type EscalateInputs struct {
	Trigger any
}

// EscalateOutputs is the typed output port surface for `kind: escalate`.
// Call ToPortValues to convert back to the wire format.
type EscalateOutputs struct {
	Result any
}

// ReadEscalateInputs extracts the typed inputs for `kind: escalate` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadEscalateInputs(pv PortValues) (EscalateInputs, error) {
	var out EscalateInputs

	if raw, ok := pv["trigger"]; ok {
		out.Trigger = raw
	}
	return out, nil
}

// ToPortValues converts EscalateOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o EscalateOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- HistoryRead port types ----

// HistoryReadInputs is the typed input port surface for `kind: history_read`.
// Use ReadHistoryReadInputs to populate it from a PortValues map.
type HistoryReadInputs struct {
	Query any
}

// HistoryReadOutputs is the typed output port surface for `kind: history_read`.
// Call ToPortValues to convert back to the wire format.
type HistoryReadOutputs struct {
	Messages []Message
}

// ReadHistoryReadInputs extracts the typed inputs for `kind: history_read` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadHistoryReadInputs(pv PortValues) (HistoryReadInputs, error) {
	var out HistoryReadInputs

	if raw, ok := pv["query"]; ok {
		out.Query = raw
	}
	return out, nil
}

// ToPortValues converts HistoryReadOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o HistoryReadOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["messages"] = o.Messages
	return pv
}

// ---- Join port types ----

// JoinInputs is the typed input port surface for `kind: join`.
// Use ReadJoinInputs to populate it from a PortValues map.
type JoinInputs struct {
	In any
}

// JoinOutputs is the typed output port surface for `kind: join`.
// Call ToPortValues to convert back to the wire format.
type JoinOutputs struct {
	Out any
}

// ReadJoinInputs extracts the typed inputs for `kind: join` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadJoinInputs(pv PortValues) (JoinInputs, error) {
	var out JoinInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts JoinOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o JoinOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- Loop port types ----

// LoopInputs is the typed input port surface for `kind: loop`.
// Use ReadLoopInputs to populate it from a PortValues map.
type LoopInputs struct {
	In any
}

// LoopOutputs is the typed output port surface for `kind: loop`.
// Call ToPortValues to convert back to the wire format.
type LoopOutputs struct {
	Out any
}

// ReadLoopInputs extracts the typed inputs for `kind: loop` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadLoopInputs(pv PortValues) (LoopInputs, error) {
	var out LoopInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts LoopOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o LoopOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- Memory port types ----

// MemoryInputs is the typed input port surface for `kind: memory`.
// Use ReadMemoryInputs to populate it from a PortValues map.
type MemoryInputs struct {
	In any
}

// MemoryOutputs is the typed output port surface for `kind: memory`.
// Call ToPortValues to convert back to the wire format.
type MemoryOutputs struct {
	Out any
}

// ReadMemoryInputs extracts the typed inputs for `kind: memory` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadMemoryInputs(pv PortValues) (MemoryInputs, error) {
	var out MemoryInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts MemoryOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o MemoryOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- Merge port types ----

// MergeInputs is the typed input port surface for `kind: merge`.
// Use ReadMergeInputs to populate it from a PortValues map.
type MergeInputs struct {
	Branch string
}

// MergeOutputs is the typed output port surface for `kind: merge`.
// Call ToPortValues to convert back to the wire format.
type MergeOutputs struct {
	Merged []Message
}

// ReadMergeInputs extracts the typed inputs for `kind: merge` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadMergeInputs(pv PortValues) (MergeInputs, error) {
	var out MergeInputs

	if raw, ok := pv["branch"]; ok {
		switch v := raw.(type) {
		case string:
			out.Branch = v
		case nil:
			// nil is treated as absent
		default:
			return out, fmt.Errorf("merge: port branch: cannot cast %T to string", raw)
		}
	} else {
		return out, fmt.Errorf("merge: required input port branch is absent")
	}
	return out, nil
}

// ToPortValues converts MergeOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o MergeOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["merged"] = o.Merged
	return pv
}

// ---- Model port types ----

// ModelInputs is the typed input port surface for `kind: model`.
// Use ReadModelInputs to populate it from a PortValues map.
type ModelInputs struct {
	Messages []Message
}

// ModelOutputs is the typed output port surface for `kind: model`.
// Call ToPortValues to convert back to the wire format.
type ModelOutputs struct {
	Response []Message
}

// ReadModelInputs extracts the typed inputs for `kind: model` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadModelInputs(pv PortValues) (ModelInputs, error) {
	var out ModelInputs

	if raw, ok := pv["messages"]; ok {
		switch v := raw.(type) {
		case []Message:
			out.Messages = v
		case nil:
			// nil is treated as absent
		default:
			return out, fmt.Errorf("model: port messages: cannot cast %T to []Message", raw)
		}
	} else {
		return out, fmt.Errorf("model: required input port messages is absent")
	}
	return out, nil
}

// ToPortValues converts ModelOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ModelOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["response"] = o.Response
	return pv
}

// ---- Parallel port types ----

// ParallelInputs is the typed input port surface for `kind: parallel`.
// Use ReadParallelInputs to populate it from a PortValues map.
type ParallelInputs struct {
	In any
}

// ParallelOutputs is the typed output port surface for `kind: parallel`.
// Call ToPortValues to convert back to the wire format.
type ParallelOutputs struct {
	Out any
}

// ReadParallelInputs extracts the typed inputs for `kind: parallel` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadParallelInputs(pv PortValues) (ParallelInputs, error) {
	var out ParallelInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts ParallelOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ParallelOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- Planner port types ----

// PlannerInputs is the typed input port surface for `kind: planner`.
// Use ReadPlannerInputs to populate it from a PortValues map.
type PlannerInputs struct {
	Task string
}

// PlannerOutputs is the typed output port surface for `kind: planner`.
// Call ToPortValues to convert back to the wire format.
type PlannerOutputs struct {
	Plan []Message
}

// ReadPlannerInputs extracts the typed inputs for `kind: planner` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadPlannerInputs(pv PortValues) (PlannerInputs, error) {
	var out PlannerInputs

	if raw, ok := pv["task"]; ok {
		switch v := raw.(type) {
		case string:
			out.Task = v
		case nil:
			// nil is treated as absent
		default:
			return out, fmt.Errorf("planner: port task: cannot cast %T to string", raw)
		}
	} else {
		return out, fmt.Errorf("planner: required input port task is absent")
	}
	return out, nil
}

// ToPortValues converts PlannerOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o PlannerOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["plan"] = o.Plan
	return pv
}

// ---- ReadBashOutput port types ----

// ReadBashOutputInputs is the typed input port surface for `kind: read_bash_output`.
// Use ReadReadBashOutputInputs to populate it from a PortValues map.
type ReadBashOutputInputs struct {
	Query any
}

// ReadBashOutputOutputs is the typed output port surface for `kind: read_bash_output`.
// Call ToPortValues to convert back to the wire format.
type ReadBashOutputOutputs struct {
	Result []Message
}

// ReadReadBashOutputInputs extracts the typed inputs for `kind: read_bash_output` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadReadBashOutputInputs(pv PortValues) (ReadBashOutputInputs, error) {
	var out ReadBashOutputInputs

	if raw, ok := pv["query"]; ok {
		out.Query = raw
	}
	return out, nil
}

// ToPortValues converts ReadBashOutputOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ReadBashOutputOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- ReadFile port types ----

// ReadFileInputs is the typed input port surface for `kind: read_file`.
// Use ReadReadFileInputs to populate it from a PortValues map.
type ReadFileInputs struct {
	Query any
}

// ReadFileOutputs is the typed output port surface for `kind: read_file`.
// Call ToPortValues to convert back to the wire format.
type ReadFileOutputs struct {
	Result []Message
}

// ReadReadFileInputs extracts the typed inputs for `kind: read_file` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadReadFileInputs(pv PortValues) (ReadFileInputs, error) {
	var out ReadFileInputs

	if raw, ok := pv["query"]; ok {
		out.Query = raw
	}
	return out, nil
}

// ToPortValues converts ReadFileOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ReadFileOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- Reflect port types ----

// ReflectInputs is the typed input port surface for `kind: reflect`.
// Use ReadReflectInputs to populate it from a PortValues map.
type ReflectInputs struct {
	Draft []Message
}

// ReflectOutputs is the typed output port surface for `kind: reflect`.
// Call ToPortValues to convert back to the wire format.
type ReflectOutputs struct {
	Critique any
	Revision []Message
}

// ReadReflectInputs extracts the typed inputs for `kind: reflect` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadReflectInputs(pv PortValues) (ReflectInputs, error) {
	var out ReflectInputs

	if raw, ok := pv["draft"]; ok {
		switch v := raw.(type) {
		case []Message:
			out.Draft = v
		case nil:
			// nil is treated as absent
		default:
			return out, fmt.Errorf("reflect: port draft: cannot cast %T to []Message", raw)
		}
	} else {
		return out, fmt.Errorf("reflect: required input port draft is absent")
	}
	return out, nil
}

// ToPortValues converts ReflectOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ReflectOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 2)
	pv["critique"] = o.Critique
	pv["revision"] = o.Revision
	return pv
}

// ---- Retry port types ----

// RetryInputs is the typed input port surface for `kind: retry`.
// Use ReadRetryInputs to populate it from a PortValues map.
type RetryInputs struct {
	In any
}

// RetryOutputs is the typed output port surface for `kind: retry`.
// Call ToPortValues to convert back to the wire format.
type RetryOutputs struct {
	Out any
}

// ReadRetryInputs extracts the typed inputs for `kind: retry` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadRetryInputs(pv PortValues) (RetryInputs, error) {
	var out RetryInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts RetryOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o RetryOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- Review port types ----

// ReviewInputs is the typed input port surface for `kind: review`.
// Use ReadReviewInputs to populate it from a PortValues map.
type ReviewInputs struct {
	Draft any
}

// ReviewOutputs is the typed output port surface for `kind: review`.
// Call ToPortValues to convert back to the wire format.
type ReviewOutputs struct {
	Verdict  any
	Approved any
}

// ReadReviewInputs extracts the typed inputs for `kind: review` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadReviewInputs(pv PortValues) (ReviewInputs, error) {
	var out ReviewInputs

	if raw, ok := pv["draft"]; ok {
		out.Draft = raw
	} else {
		return out, fmt.Errorf("review: required input port draft is absent")
	}
	return out, nil
}

// ToPortValues converts ReviewOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ReviewOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 2)
	pv["verdict"] = o.Verdict
	pv["approved"] = o.Approved
	return pv
}

// ---- SessionWrite port types ----

// SessionWriteInputs is the typed input port surface for `kind: session_write`.
// Use ReadSessionWriteInputs to populate it from a PortValues map.
type SessionWriteInputs struct {
	Text any
}

// SessionWriteOutputs is the typed output port surface for `kind: session_write`.
// Call ToPortValues to convert back to the wire format.
type SessionWriteOutputs struct {
	MessageId any
	Appended  any
}

// ReadSessionWriteInputs extracts the typed inputs for `kind: session_write` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadSessionWriteInputs(pv PortValues) (SessionWriteInputs, error) {
	var out SessionWriteInputs

	if raw, ok := pv["text"]; ok {
		out.Text = raw
	}
	return out, nil
}

// ToPortValues converts SessionWriteOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o SessionWriteOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 2)
	pv["message_id"] = o.MessageId
	pv["appended"] = o.Appended
	return pv
}

// ---- Sleep port types ----

// SleepInputs is the typed input port surface for `kind: sleep`.
// Use ReadSleepInputs to populate it from a PortValues map.
type SleepInputs struct {
	Args any
}

// SleepOutputs is the typed output port surface for `kind: sleep`.
// Call ToPortValues to convert back to the wire format.
type SleepOutputs struct {
	Result any
}

// ReadSleepInputs extracts the typed inputs for `kind: sleep` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadSleepInputs(pv PortValues) (SleepInputs, error) {
	var out SleepInputs

	if raw, ok := pv["args"]; ok {
		out.Args = raw
	}
	return out, nil
}

// ToPortValues converts SleepOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o SleepOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- SubagentDispatch port types ----

// SubagentDispatchInputs is the typed input port surface for `kind: subagent_dispatch`.
// Use ReadSubagentDispatchInputs to populate it from a PortValues map.
type SubagentDispatchInputs struct {
	Args any
}

// SubagentDispatchOutputs is the typed output port surface for `kind: subagent_dispatch`.
// Call ToPortValues to convert back to the wire format.
type SubagentDispatchOutputs struct {
	Result any
}

// ReadSubagentDispatchInputs extracts the typed inputs for `kind: subagent_dispatch` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadSubagentDispatchInputs(pv PortValues) (SubagentDispatchInputs, error) {
	var out SubagentDispatchInputs

	if raw, ok := pv["args"]; ok {
		out.Args = raw
	}
	return out, nil
}

// ToPortValues converts SubagentDispatchOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o SubagentDispatchOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- Tool port types ----

// ToolInputs is the typed input port surface for `kind: tool`.
// Use ReadToolInputs to populate it from a PortValues map.
type ToolInputs struct {
	Args any
}

// ToolOutputs is the typed output port surface for `kind: tool`.
// Call ToPortValues to convert back to the wire format.
type ToolOutputs struct {
	Result any
}

// ReadToolInputs extracts the typed inputs for `kind: tool` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadToolInputs(pv PortValues) (ToolInputs, error) {
	var out ToolInputs

	if raw, ok := pv["args"]; ok {
		out.Args = raw
	}
	return out, nil
}

// ToPortValues converts ToolOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ToolOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["result"] = o.Result
	return pv
}

// ---- ToolDispatch port types ----

// ToolDispatchInputs is the typed input port surface for `kind: tool_dispatch`.
// Use ReadToolDispatchInputs to populate it from a PortValues map.
type ToolDispatchInputs struct {
	ToolCalls any
}

// ToolDispatchOutputs is the typed output port surface for `kind: tool_dispatch`.
// Call ToPortValues to convert back to the wire format.
type ToolDispatchOutputs struct {
	ToolResults any
	Messages    []Message
}

// ReadToolDispatchInputs extracts the typed inputs for `kind: tool_dispatch` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadToolDispatchInputs(pv PortValues) (ToolDispatchInputs, error) {
	var out ToolDispatchInputs

	if raw, ok := pv["tool_calls"]; ok {
		out.ToolCalls = raw
	} else {
		return out, fmt.Errorf("tool_dispatch: required input port tool_calls is absent")
	}
	return out, nil
}

// ToPortValues converts ToolDispatchOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o ToolDispatchOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 2)
	pv["tool_results"] = o.ToolResults
	pv["messages"] = o.Messages
	return pv
}

// ---- TraceWrite port types ----

// TraceWriteInputs is the typed input port surface for `kind: trace_write`.
// Use ReadTraceWriteInputs to populate it from a PortValues map.
type TraceWriteInputs struct {
	In any
}

// TraceWriteOutputs is the typed output port surface for `kind: trace_write`.
// Call ToPortValues to convert back to the wire format.
type TraceWriteOutputs struct {
	Ack any
}

// ReadTraceWriteInputs extracts the typed inputs for `kind: trace_write` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadTraceWriteInputs(pv PortValues) (TraceWriteInputs, error) {
	var out TraceWriteInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	}
	return out, nil
}

// ToPortValues converts TraceWriteOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o TraceWriteOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["ack"] = o.Ack
	return pv
}

// ---- Transform port types ----

// TransformInputs is the typed input port surface for `kind: transform`.
// Use ReadTransformInputs to populate it from a PortValues map.
type TransformInputs struct {
	In any
}

// TransformOutputs is the typed output port surface for `kind: transform`.
// Call ToPortValues to convert back to the wire format.
type TransformOutputs struct {
	Out any
}

// ReadTransformInputs extracts the typed inputs for `kind: transform` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadTransformInputs(pv PortValues) (TransformInputs, error) {
	var out TransformInputs

	if raw, ok := pv["in"]; ok {
		out.In = raw
	} else {
		return out, fmt.Errorf("transform: required input port in is absent")
	}
	return out, nil
}

// ToPortValues converts TransformOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o TransformOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["out"] = o.Out
	return pv
}

// ---- WriteFile port types ----

// WriteFileInputs is the typed input port surface for `kind: write_file`.
// Use ReadWriteFileInputs to populate it from a PortValues map.
type WriteFileInputs struct {
	Payload any
}

// WriteFileOutputs is the typed output port surface for `kind: write_file`.
// Call ToPortValues to convert back to the wire format.
type WriteFileOutputs struct {
	Ack bool
}

// ReadWriteFileInputs extracts the typed inputs for `kind: write_file` from pv.
// Required ports that are absent in pv return a descriptive error.
// The function never mutates pv.
func ReadWriteFileInputs(pv PortValues) (WriteFileInputs, error) {
	var out WriteFileInputs

	if raw, ok := pv["payload"]; ok {
		out.Payload = raw
	}
	return out, nil
}

// ToPortValues converts WriteFileOutputs to the PortValues wire format.
// Fields with zero values are included; callers can remove unwanted keys
// after the fact using PortValues.Clone + delete.
func (o WriteFileOutputs) ToPortValues() PortValues {
	pv := make(PortValues, 1)
	pv["ack"] = o.Ack
	return pv
}
