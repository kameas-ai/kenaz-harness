# Seam fanout fixture for kind=session_write
# HistoryWriter.AppendEntry is called to persist the message.
kind: session_write
seams:
  - seam: HistoryWriter.AppendEntry
    required: true
notes: "session_write calls HistoryWriter.AppendEntry with role and content from ports (move metadata stays zero until WP02)"
