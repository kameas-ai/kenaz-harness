# Seam fanout fixture for kind=session_write
# HistoryWriter.AppendMessage is called to persist the message.
kind: session_write
seams:
  - seam: HistoryWriter.AppendMessage
    required: true
notes: "session_write calls HistoryWriter.AppendMessage with role and content from ports"
