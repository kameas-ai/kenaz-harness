# Seam fanout fixture for kind=approval
# approval-node-01PMZC12 UNIT-2: the pending decision is registered on
# AskBus.Pending on the SAME (first, pausing) fire, mirroring `ask`;
# the run then parks via ErrPaused. UNIT-3 has not landed a resolve
# verb yet, so there is no resumed-fire lookup today — every fire
# registers-and-parks again.
kind: approval
seams:
  - seam: AskBus.Pending
    required: true
notes: "approval node registers the pending decision on AskBus.Pending, then parks the run (ErrPaused), on its first fire; no resolve verb exists yet (UNIT-3), so it re-parks on any subsequent fire"
