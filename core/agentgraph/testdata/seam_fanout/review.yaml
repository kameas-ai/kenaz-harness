# Seam fanout fixture for kind=review
# LLMProvider.Generate is called to evaluate the review criteria.
kind: review
seams:
  - seam: LLMProvider.Generate
    required: true
notes: "review node calls LLMProvider.Generate with a review prompt; result gates continuation"
