# Seam fanout fixture for kind=read_file
# PolicyGate.CheckFileRead is called before reading the file.
kind: read_file
seams:
  - seam: PolicyGate.CheckFileRead
    required: true
notes: "read_file calls PolicyGate.CheckFileRead before opening the path; gated by the policy seam"
