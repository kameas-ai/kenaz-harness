# Invalid: two nodes share the same id. Validator rejects.
spec_version: "1"
id: "invalid_dup_id"
entrypoints:
  - dup
nodes:
  - id: dup
    kind: model
    attrs:
      model: m
  - id: dup
    kind: transform
    attrs:
      name: noop
