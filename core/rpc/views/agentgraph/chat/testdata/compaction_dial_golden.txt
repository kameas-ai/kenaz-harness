# Compaction dial — observable behaviour per tier per scenario.
# Generated by TestCompactionDial_Golden (WP07,
# agentgraph-total-convergence-01PMGX01 Phase 4). Each block records the
# compaction.SessionEngine calls the dial makes, in order, with full arguments,
# followed by the error returned to the chat surface.
#
# Session fixture: one persisted user message sized to the stated token
# count, plus the new user turn "the next user turn".

================================================================
scenario: far-under-trigger
  history_tokens=10 cap=100000 deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: <nil>

tier=conservative
  engine calls: (none)
  returned error: <nil>

tier=balanced
  engine calls: (none)
  returned error: <nil>

tier=aggressive
  engine calls: (none)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: at-70pct-of-cap
  history_tokens=65 cap=100 deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: <nil>

tier=conservative
  engine calls: (none)
  returned error: <nil>

tier=balanced
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.30)
  returned error: <nil>

tier=aggressive
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.40)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: at-90pct-of-cap
  history_tokens=85 cap=100 deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=conservative
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.20)
  returned error: <nil>

tier=balanced
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.30)
  returned error: <nil>

tier=aggressive
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.40)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: over-cap
  history_tokens=150 cap=100 deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=conservative
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.20)
  returned error: <nil>

tier=balanced
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.30)
  returned error: <nil>

tier=aggressive
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.40)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: unknown-model-cap
  history_tokens=150 cap=unknown deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: <nil>

tier=conservative
  engine calls: (none)
  returned error: <nil>

tier=balanced
  engine calls: (none)
  returned error: <nil>

tier=aggressive
  engine calls: (none)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: no-cap-lookup-dep
  history_tokens=150 cap=no-lookup-dep deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: <nil>

tier=conservative
  engine calls: (none)
  returned error: <nil>

tier=balanced
  engine calls: (none)
  returned error: <nil>

tier=aggressive
  engine calls: (none)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: engine-model-too-small
  history_tokens=85 cap=100 deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=conservative
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.20)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=balanced
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.30)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=aggressive
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.40)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
    2. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.40)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

================================================================
scenario: engine-generic-error
  history_tokens=85 cap=100 deps=wired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: session has hit its context window and compaction is unavailable [is:ErrSessionFull]

tier=conservative
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.20)
  returned error: <nil>

tier=balanced
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.30)
  returned error: <nil>

tier=aggressive
  engine calls:
    1. Compact(session="session-1", model="profile-1:model-1", oldest_pct=0.40)
  returned error: <nil>

tier=maximal
  engine calls:
    1. RollingSummarize(session="session-1", model="profile-1:model-1", recent_window=4)
  returned error: <nil>

================================================================
scenario: env-var-off
  history_tokens=150 cap=100 deps=wired env_off=true
================================================================

tier=off
  engine calls: (none)
  returned error: <nil>

tier=conservative
  engine calls: (none)
  returned error: <nil>

tier=balanced
  engine calls: (none)
  returned error: <nil>

tier=aggressive
  engine calls: (none)
  returned error: <nil>

tier=maximal
  engine calls: (none)
  returned error: <nil>

================================================================
scenario: no-compaction-deps
  history_tokens=150 cap=100 deps=unwired env_off=false
================================================================

tier=off
  engine calls: (none)
  returned error: <nil>

tier=conservative
  engine calls: (none)
  returned error: <nil>

tier=balanced
  engine calls: (none)
  returned error: <nil>

tier=aggressive
  engine calls: (none)
  returned error: <nil>

tier=maximal
  engine calls: (none)
  returned error: <nil>
