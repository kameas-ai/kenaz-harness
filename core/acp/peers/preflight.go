package peers

// preflight.go is reserved for additional preflight helpers (see
// PreflightAll on Registry). For now the public preflight surface is
// the registry method itself; this file exists in line with the WP04
// file plan so the bundle resolver has a stable import target when
// per-peer preflight logic grows beyond the registry method.
